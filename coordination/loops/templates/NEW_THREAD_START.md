# New Thread Start

You are starting a prompt-controlled loop thread for GenesisVLA governance.

Read in order:

1. `AGENTS.md`
2. `boundaries.txt`
3. `docs/coordination/CODEX_MANAGER_GOVERNANCE.md`
4. `docs/coordination/PROMPT_CONTROLLED_LOOP_PROTOCOL.md`
5. `docs/coordination/NEW_THREAD_BOOTSTRAP.md`
6. assigned loop spec

Active model label: `gpt-5.5`.

Proceed from the top-level prompt. Ask the user only when a required field, authorization, budget policy, timeout policy, validation evidence path, connector action, compute action, or publication gate is missing or ambiguous.

If blocked, report `BLOCKED_LOOP_SPEC` with the missing field list.
