data:
  schema_version: "1.0"
  name: robodm-container-explicit-local
  root: datasets/working/robodm_container
  required_modalities:
    - camera.rgb_0
    - camera.rgb_1
    - camera.rgb_2
  datasets:
    - name: robodm-container-explicit-local
      backend: robodm_container
      root: datasets/working/robodm_container
      split: train
      access_mode: map
      image_keys:
        - camera.rgb_0
        - camera.rgb_1
        - camera.rgb_2
