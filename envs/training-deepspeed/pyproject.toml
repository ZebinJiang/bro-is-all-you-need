[project]
name = "training-deepspeed"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = ["autovla[training-deepspeed,model-gr00t-n1d6,data-webdataset]"]

[tool.uv.sources]
autovla = {path = "../..", editable = true}

[tool.autovla]
profile = "training-deepspeed"
default-sync = "manual"
