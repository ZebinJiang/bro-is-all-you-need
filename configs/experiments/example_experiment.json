{
  "schema_version": "1.0",
  "experiment_key": "mock_genesisvla_sandbox_check",
  "description": "Mock experiment config used by sandbox smoke test; it does not load a real VLA checkpoint, dataset, robot endpoint, or serving endpoint.",
  "mode": "mock",
  "baseline_reference": "registered-vla-baseline",
  "dataset_reference": null,
  "model_or_backbone_reference": null,
  "tokenizer_reference": null,
  "parameters": {
    "example_value": 3,
    "strict_mode": true
  },
  "output_policy": {
    "write_root_env": "SANDBOX_RUN_DIR",
    "allow_absolute_output_paths": false
  }
}
